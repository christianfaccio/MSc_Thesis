% generated by stats/plot_training_curves.py -- do not edit by hand
\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{assets/fig7_training_curves_N4.pdf}
\caption[Training curves ($N = 4$)]{Training curves for the four policies at $N = 4$. Curves are a rolling mean over 200 episodes; shaded: $\pm1$ s.d. of the episodes in the window. Because the three charts are logged once per episode, the band measures how much individual episodes differ from one another at that point in training; each configuration was trained with a single seed, so it is \emph{not} an across-seed error bar and says nothing about run-to-run reproducibility.\label{fig:fig7_training_curves_N4}}
\end{figure}
